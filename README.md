#Техническое задание:

##Уровень доступа – администратор (Administrator)

###Заголовок страницы: Страница доступа

На странице необходимо отобразить форму для входа на веб версию сайта

Отображаться должны следующие данные:

    - адрес электронной почты (email)
    - пароль (password)

###Заголовок страницы: Пользователи

На странице необходимо отобразить список пользователей.

Страница доступна только для пользователя с привилегией Administrator.

Отображаться должны следующие данные:

    - ФИО
    - адрес электронноп почты
    - роль

1. Записи должны быть отсортированы по email в алфавитном порядке.
2. Если записей более 10, должен осуществляться постраничный вывод.
3. Для каждой записи должна быть возможность пометки на удаление (чекбокс).
4. Под списком-таблиц должна быть кнопка “Удалить”, по нажатию на которую все помеченные записи должны быть удалены. Один из администраторов с ролью “администратор” должен быть недоступен для удаления (и ему должна быть заблокирована возможность “уменьшить” привилегии), чтобы не было возможности удалить всех пользователей.
5. Для каждой записи должна быть возможность изменения пароля, который генерируется автоматически произвольно и отправляется на email пользователя.
6. Для каждой записи должна быть возможность изменения привилегий.
 
###Заголовок страницы: Добавление пользователи

На странице необходимо отобразить форму для добавления пользователя.

Страница доступна только для пользователя с привилегией Administrator.
Заполняться должны следующие данные:

    - Фамилия(40 символов, только латинские буквы)
    - Имя(20 символов, только латинские буквы)
    - Отчество(40 символов, только латинские буквы)
    - Email(50 символов, стандартный шаблон)
    - Role(Administrator, Sale User, Customer User, Secure API User)
 
###Заголовок страницы: Отзывы

На странице должен быть отображен список отзывов.

Отображаться должны следующие данные:

    - ФИО
    - Отзыв
    - Дата добавления
    - Статус “Показывать” (чекбокс)
1. Если записей более 10, должен осуществляться постраничный вывод.
2. Также на данной странице должна быть возможность удалить отзыв.

---

######Sale author

    - Articles: create, update, delete(soft delete), delete(soft delete) comments.
    - Items: create, copy, delete(soft delete), upload with help of xml
    - Orders: show orders, change status(NEW, REVIEWING, IN_PROGRESS, DELIVERED)

######Customer author

    - Articles: show, create comment for articles.
    - Orders: create order with one item and some quantity, show orders
    - Profile: change information except email, change password
	
---

##Уровень доступа – покупатель (Customer)

###Общие параметры

1. На страницы должны содержать информацию о пользователе (имя, фамилию, адрес электронной почты).
2. Страницы должны предоставлять возможность выхода пользователя из сайта (Logout).
3. Страницы должны предоставлять возможность для навигации по сайту.
 
###Заголовок страницы: Страница доступа (Login)

На странице необходимо отобразить форму для входа на веб версию сайта

Отображаться должны следующие данные:

    - адрес электронной почты (email)(50 символов, стандартный шаблон)
    - пароль (password)(символы должны скрываться)
    
1. Должна присутствовать валидация вводимых данных.
 
###Заголовок страницы: Страница регистрации (Registration)

На странице необходимо отобразить форму для регистрации на сайте

Должны присутствовать следующие поля:

    - имя (first name)(20 символов, только латинские буквы)*
    - фамилия (second name)(40 символов, только латинские буквы)*
    - отчество(40 символов, только латинские буквы)
    - адрес электронной почты (email)(50 символов, стандартный шаблон)*
       
1. Поля, отмеченные звездой, должны быть обязательны для заполнения.
2. Должна присутствовать валидация вводимых данных.
 
###Заголовок страницы: Статьи (Articles).

На странице должен отображаться список статей на сайте.
Каждая позиция должен содержать следующие данные:

    - заголовок
    - краткое содержание (information)
    - дата публикации (date)
    - количество комментариев (comments)

1. Должен быть предусмотрен постраничный вывод списка статей.
2. Должен быть предусмотрен поиск статей по заголовку, диапазону даты публикации.
 
###Заголовок страницы: Статья (Article).

На странице должна отображаться статья и комментарии к ней.

1. Пользователь должен иметь возможность комментирования статьи.(1000 символов, только латинские буквы)
 
###Заголовок страницы: Товары (Items).

На странице необходимо отобразить список доступных для покупки товаров.

Товар должен содержать следующую информацию:

    - наименование (name)
    - категория (category)
    - описание (description)
    - цена (price)

1. Должна присутствовать возможность добавления товара в корзину с указанием количества в диапазоне от 0 - 100.
2. Должна присутствовать возможность сортировки товара по алфавиту, цене (возрастанию и убыванию).
3. Должен быть предусмотрен постраничный вывод списка товаров.
4. Должен быть предусмотрен поиск товаров по наименованию.
5. Должен быть предусмотрен вариант выбора товара по категории и диапазону цен.

###Заголовок страницы: Новый заказ (New order).

На странице должен отображаться список всех товаров, добавленных пользователем.

Каждая позиция должна содержать следующие пункты:

    - порядковый номер
    - наименование (name)
    - количество (amount)
    - цена за 1 штуку (price)
    - итоговая цена за позицию (total price)

1. На странице должна присутствовать информация о итоговой стоимости всех позиций (order price).
2. Должна быть возможность изменения количества товара для каждой позиции.
3. При количестве товара равному 0, товар должен удаляться из корзины.
4. Должна присутствовать возможность обновления данных.
5. Должен быть возможность указания адреса доставки.
6. Должна быть возможность указания комментария к заказу.
7. Должен быть функционал оформления заказа.
 
###Заголовок страницы: Заказ (Order).

На странице должна отображаться итоговая информация о заказе:

    - наименование (name)
    - количество (amount)
    - цена за 1 штуку (price)
    - итоговая цена за позицию (total price)
    - итоговая стоимость заказа (order price)
    - информация о получателе (customer)
    - информация об адресе доставки (address)
    - комментарий (comment)
    - дата оформления (date)
    - статус (status) (NEW, REVIEWING, IN_PROGRESS, DELIVERED)
 
###Заголовок страницы: История заказов (Order history).

На странице должна отображаться список всех заказов покупателя.

Каждая позиция должен содержать следующие данные:

    - порядковый номер
    - краткая информация (information)
    - общая стоимость (price)
    - дата оформления (date)
    - статус (status)

1. Должна присутствовать возможность просмотреть каждый заказ подробнее (переход на его страницу).
2. Должна присутствовать возможность сортировки по дате и статусу.
3. Должен быть предусмотрен постраничный вывод списка заказов.
 
###Заголовок страницы: Личный кабинет (Profile).

На странице должна отображаться информация о пользователе.

Должна присутствовать следующая информация:

    - персональный номер (id)
    - имя (first name)
    - фамилия (second name)
    - отчество
    - адрес электронной почты (email)
    - адрес доставки (delivery address)
1. Должна присутствовать возможность смены имени, фамилии, отчества, адреса доставки.
2. Должна присутствовать возможность смены пароля.
3. Должна присутствовать возможность удаления аккаунта.
  
##Уровень доступа – продавец (Seller)

###Общие параметры

1. На страницы должны содержать информацию о пользователе (имя, фамилию, адрес электронной почты).
2. Страницы должны предоставлять возможность выхода пользователя из сайта (Logout).
3. Страницы должны предоставлять возможность для навигации по сайту.
 
###Заголовок страницы: Страница доступа (Login)

На странице необходимо отобразить форму для входа на веб версию сайта

Отображаться должны следующие данные:

    - адрес электронной почты (email)(50 символов, стандартный шаблон)
    - пароль (password)(должен скрываться)
1. Должна присутствовать валидация вводимых данных.
 
###Заголовок страницы: Cтатьи (Articles)

На странице должен отображаться список статей на сайте.

Каждая позиция должен содержать следующие данные:

    - заголовок
    - краткое содержание (information)
    - дата публикации (price)
    - количество комментариев (date)
1. Должна присутствовать скрытия статьи из публикуемых.
2. Должен быть предусмотрен постраничный вывод списка статей.
 
###Заголовок страницы: Статья (Article).

На странице должна отображаться статья и комментарии к ней.

1. Пользователь должен иметь возможность редактирования статьи.
2. Пользователь должен иметь возможность удаления статьи.
3. Пользователь должен иметь возможность удаления комментариев к статье.
 
###Заголовок страницы: Новая статья (New article).

На странице должны присутствовать элементы для создания статей на сайте.

Должны присутствовать следующие поля:

    - заголовок (150 символов, только латинские буквы)*
    - содержание (3000 символов, только латинские буквы)*
    - краткое содержание (400 символов, только латинские буквы)*

1. Поля отмеченные звездой обязательны для заполнения.
2. Для формирования краткой информации должна быть возможность копирования требуемого количества символов из содержания.
 
###Заголовок страницы: Товары (Items).

На странице необходимо отобразить список товаров выставленных на продажу.

Товар должен содержать следующую информацию:

    - наименование (name)
    - категория (category)
    - описание (description)
    - цена (price)

1. Должна присутствовать возможность добавления товара в список на удаление.
2. Должна присутствовать возможность сортировки товара по алфавиту, цене.
3. Должна присутствовать возможность копирования товара.
4. Должна присутствовать возможность загрузки товара из внешнего файла.
5. Должен быть предусмотрен постраничный вывод списка товаров.
 
###Заголовок страницы: Новый товар (New item).

На странице должны присутствовать элементы для создания товара в каталоге.

Товар должен содержать следующую информацию:

    - наименование (name)(50 символов, только латинские буквы)*
    - категория (category)(40 символов, только латинские буквы)*
    - описание (description)(200 символов, только латинские буквы)
    - цена (price)(в диапазоне 0 - 1 000 000)*

1. Должна присутствовать валидация полей.
2. Все поля отмеченные * обязательны к заполнению.
 
###Заголовок страницы: Заказы (Orders).

На странице должна отображаться список всех заказов на сайте.

Каждая позиция должен содержать следующие данные:

    - порядковый номер
    - имя и фамилия покупателя
    - адрес электронной почты
    - адрес доставки
    - наименования (information)
    - комментарий к заказу
    - общая стоимость (price)
    - дата оформления (date)
    - статус (status)

1. Должна присутствовать возможность просмотреть каждый заказ подробнее (переход на его страницу).
2. Должна присутствовать возможность сортировки по дате и статусу.
3. Должна присутствовать возможность изменения статуса заказа.
4. Должен быть предусмотрен постраничный вывод списка заказов.
 
###Заголовок страницы: Личный кабинет (Profile).

На странице должна отображаться информация о пользователе.

Должна присутствовать следующая информация:

    - персональный номер (id)
    - имя (first name)
    - фамилия (second name)
    - отчество
    - адрес электронной почты (email)

1. Должна присутствовать возможность смены имени, фамилии, отчества.
2. Должна присутствовать возможность установки нового пароля.

